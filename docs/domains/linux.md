# Linux domain

## 范围（Scope）

- Linux 命令行与常见排障/运维流程（偏“通用技能”，避免发行版强绑定）

## 来源策略（Source policy）

优先使用：

- man pages（可用 man7.org 镜像链接）
- 官方文档（GNU/coreutils/OpenSSH/systemd 等）
- 发行版文档（Ubuntu/Debian/RedHat/Arch 等）

## 禁止事项（Out of scope）

- 未授权入侵/漏洞利用/绕过安全控制（见 `SAFETY.md`）
- 破坏性默认行为（高风险必须强提示 + 预演 + 确认）

## Owners

TODO: 指定 owner（可为 intern）

