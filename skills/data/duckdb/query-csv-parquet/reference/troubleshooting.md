# Troubleshooting

## CSV 乱码/分隔符不对
- 处理：先用 `read_csv_auto` 的抽样结果确认编码与分隔符；必要时显式指定分隔符/引号/escape

## 日期/数值类型推断错误
- 处理：用 `DESCRIBE` 看推断 schema；必要时在读取时指定列类型或在 SQL 中显式 `CAST`

## 导出文件为空或路径不对
- 处理：确认当前工作目录与输出路径；避免误写到临时目录或覆盖重要文件
