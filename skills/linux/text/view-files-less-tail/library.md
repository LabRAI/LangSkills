# Library

## Copy-paste commands

```bash
less -N /var/log/syslog
head -n 50 /etc/ssh/sshd_config
tail -n 200 /var/log/syslog
tail -F /var/log/nginx/access.log
```

## Prompt snippet

```text
Given a log file path, suggest a safe less/tail workflow to find errors and follow new lines.
```
