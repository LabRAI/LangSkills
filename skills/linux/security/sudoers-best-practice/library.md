# Library

## Copy-paste commands

```bash
sudo visudo -f /etc/sudoers.d/deploy
sudo -l -U deploy
```

## Prompt snippet

```text
Given a required admin task, write a least-privilege sudoers rule using visudo, and include verification commands. Warn about dangerous patterns.
```
