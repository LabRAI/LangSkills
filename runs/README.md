# Runs

本目录用于保存 crawler/extractor/orchestrator 的运行产物（`runs/<run-id>/`）。

- 默认情况下，除少量示例外，`runs/` 下的内容都会被 `.gitignore` 忽略（避免把运行状态与大文件提交到 git）。
- 本仓库保留了少量 **示例产物**（用于离线查看 README 里提到的文件结构）：
  - `runs/linux-crawl-demo/`
  - `runs/linux-orch-weekly/`
  - `runs/integrations-weekly/`

